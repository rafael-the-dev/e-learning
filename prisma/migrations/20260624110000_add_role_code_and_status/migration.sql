BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[roles] ADD [code] NVARCHAR(1000),
[status] NVARCHAR(1000) NOT NULL CONSTRAINT [roles_status_df] DEFAULT 'ACTIVE';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH

